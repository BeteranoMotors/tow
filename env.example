# Este proyecto no necesita variables para el build local.
# Si vas a usar Firebase embebido en el HTML del host app-shell,
# no es necesario .env aquí.
# Si más adelante expones claves públicas (p.ej. Google Maps),
# crea VITE_* y usa import.meta.env en el bundle.
# VITE_OSRM_URL=https://router.project-osrm.org/route/v1
